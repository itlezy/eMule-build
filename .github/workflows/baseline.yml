name: Baseline

on:
  push:
  pull_request:

jobs:
  baseline:
    uses: eMulebb/eMule-tooling/.github/workflows/reusable-baseline.yml@ci/v8
    with:
      policy_path: manifests/privacy-guard/policy.v1.json
      repo_kind: generic
      extra_step_script: .github/scripts/baseline-extra.cmd
      tooling_ref: ci/v8
