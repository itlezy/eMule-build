name: Baseline

on:
  push:
  pull_request:

jobs:
  baseline:
    uses: eMulebb/eMule-tooling/.github/workflows/reusable-baseline.yml@main
    with:
      policy_path: manifests/privacy-guard/policy.v1.json
      repo_kind: generic
      extra_commands: |
        python -m pip install -e .[dev]
        python -m pytest
      tooling_ref: main
